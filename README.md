# Web-application "заметки"

## Available Scripts

In the project directory, you can run for start frontend:

### `npm start`

For start backend, you can run:

### `npm /backend/scr/index`

database dump attached: todolist.sql
